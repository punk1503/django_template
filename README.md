# Django web app tempalte

### Installation (MacOS)
```
python3 -m venv venv_name
source venv_name/bin/activate
pip install -r requirements.txt
python3 manage.py migrate
```

### Run (MacOS)
```python3 manage.py runserver```